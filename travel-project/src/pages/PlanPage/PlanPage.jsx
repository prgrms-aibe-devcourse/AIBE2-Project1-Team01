import React, { useState } from 'react';
import DatePickerModal from '../../components/DatePickerModal';

const PlanPage = () => {
    return (
        <div>
            <DatePickerModal />
        </div>
    );
};